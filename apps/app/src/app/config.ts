export const Config = {
  getLabel: () => $localize`I'm returned from a config file object method`,
  label: $localize`I'm from a config file object property`,
}

export const MyConstant = $localize`I'm from an exported const`
